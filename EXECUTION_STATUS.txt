WHERE WE ARE NOW
Recent hardening and validation slices are merged to dev, including the tray wakeup/menu reliability fix that keeps tray-originated actions from stalling behind `WaitMessage()`.
That tray fix is finished and validated with `cargo check`, `cargo test`, `cargo build --release`, and `powershell -ExecutionPolicy Bypass -File tests\smoke_test.ps1`.

WHAT JUST FINISHED
`fix/tray-menu-wakeup` is complete and merged to dev.
It wires tray menu actions to wake the main thread promptly, preserves the existing IPC wake path, and adds regression coverage for issuing exactly one wake request per forwarded action batch.

WHAT COMES NEXT
The remaining work is the manual and elevated verification still called out in `docs\MVP_VALIDATION_MATRIX.md`.
Focus on the real-Windows-session checks there, especially tray, Explorer-restart, autostart, shutdown, and other admin-sensitive scenarios.
